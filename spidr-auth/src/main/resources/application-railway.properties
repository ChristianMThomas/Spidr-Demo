spring.mongodb.uri=${MONGO_URI}

resend.api-key=${RESEND_API_KEY:}
mail.from=${MAIL_FROM:noreply@spidrapp.com}

# Production: do not accept CORS requests from localhost origins.
app.cors.allow-local-dev=false
