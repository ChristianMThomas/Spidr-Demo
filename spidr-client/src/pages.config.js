/**
 * pages.config.js
 *
 * The previous auto-generated PAGES map is no longer used. With the routing
 * migration (each top-level page now has its own dedicated route in App.jsx)
 * the App.jsx file directly imports the pages it routes to with React.lazy().
 *
 * This file is kept as a stub for backwards compatibility — any old code
 * that still imports `pagesConfig` will get a no-op shape. New code should
 * register routes directly in App.jsx.
 */
import __Layout from './Layout.jsx';

export const PAGES = {};

export const pagesConfig = {
  mainPage: 'home',
  Pages: PAGES,
  Layout: __Layout,
};
