# Override the URLs in app.json -> "extra" by editing app.json directly.
# This file is a placeholder; the mobile app reads from expo-constants
# (app.json -> extra), not from process.env at runtime.
